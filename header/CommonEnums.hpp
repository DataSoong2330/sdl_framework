#ifndef __ENUMS__
#define __ENUMS__

enum class FLIP { NONE, FLIP_HORIZONTAL, FLIP_VERTICAL };
enum class TEXTQUALITY { SOLID, SHADED, BLENDED };
enum class States { None, Menu, Play, Pause, GameOver, Setting };
enum class StateAction { Push, Pop, Clear };

#endif // __ENUMS__
